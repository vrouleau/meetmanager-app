services:
  backend:
    build: ./backend
    image: meetmanager-backend:test
    ports:
      - "8000:8000"
    volumes:
      - ./templates:/app/templates:ro

  frontend:
    build: ./frontend
    image: meetmanager-frontend:test
